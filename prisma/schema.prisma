// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model User {
  idUser     String     @id @default(uuid())
  email      String     @unique
  imgProfile String
  username   String?    @unique
  name       String?
  balances   Balance[]
  movements  Movement[]
}

model Balance {
  idBalance Int    @id @default(autoincrement())
  userId    String
  month     Int
  year      Int
  earnings  Float? // Utiliza Float para representar valores con decimales
  expenses  Float? // Utiliza Float para representar valores con decimales
  User      User?  @relation(fields: [userId], references: [idUser])
}

model Movement {
  idMovement   Int          @id @default(autoincrement())
  typeId       Int
  userId       String
  createdAt    DateTime
  description  String
  value        Float
  User         User         @relation(fields: [userId], references: [idUser])
  TypeMovement TypeMovement @relation(fields: [typeId], references: [idTypeMovement])
}

model TypeMovement {
  idTypeMovement Int        @id @default(autoincrement())
  description    String
  movements      Movement[]
}
